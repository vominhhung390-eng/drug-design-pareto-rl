# Rebuilt experiment outputs

This directory is intentionally free of bulky historical runs. `scripts/reproduce_all.ps1` recreates bottom-model checkpoints, 120 formal method/target/seed runs, ablations, evaluations, docking inputs and paper tables here or in the documented docking output directory.

Compact verified reference summaries are retained under `reference_results/`.
